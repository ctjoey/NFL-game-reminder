A new app icon, and that's the whole update.

Nothing else changed. Everything from the last release — live scores and the quarter while the games are on, final scores, and both teams' records on every matchup — works exactly as it did.
