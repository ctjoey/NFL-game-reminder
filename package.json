{
  "name": "nfl-game-reminder",
  "version": "1.0.0",
  "description": "Ad-free NFL game reminders that tell you when coverage starts, when kickoff is, which network or streaming service has the game, and what channel that is in your market.",
  "type": "module",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "launch": "node launch.js",
    "dev": "node --watch server/index.js",
    "test": "node --test",
    "sync": "node server/schedule/sync-cli.js",
    "vapid": "node server/notify/gen-vapid.js"
  },
  "engines": { "node": ">=20" },
  "license": "MIT",
  "dependencies": {
    "express": "^4.21.2",
    "web-push": "^3.6.7"
  }
}
